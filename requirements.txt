# ============================================================
# KNHANES Stratified Health Grade — requirements.txt
# Python >= 3.9 권장
# ============================================================

# 데이터 처리
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0          # parquet 읽기/쓰기 (pandas 의존)
pyreadstat>=1.2.0        # SAS (.sas7bdat) 파일 읽기

# 머신러닝
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
optuna>=3.3.0            # 베이즈 하이퍼파라미터 최적화

# 설명 가능한 AI
shap>=0.43.0

# 모델 저장
joblib>=1.3.0            # scikit-learn 설치 시 자동 포함

# 시각화
matplotlib>=3.7.0